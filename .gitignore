# Build artifacts
__pycache__/
*.pyc
*.pyo
*.so
*.o
build/
dist/
*.egg-info/
.eggs/

# Virtual environments
.venv/
venv/
env/
.env/

# uv (pip-interface workflow; reproducibility is anchored by pinned submodules,
# not a locked env, so the auto-generated lock is not tracked)
uv.lock

# Large / regenerable data files
*.npy
*.pt
*.pkl

# Local data storage (root data/ only — src/bondmaxsim/data is a package)
/data/
*.csv
*.png

# results/ is TRACKED — all outputs are research evidence (see docs/project_structure.md).
!results/
!results/**