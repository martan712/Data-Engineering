# Build artifacts
__pycache__/
*.pyc
*.pyo
*.so
*.o
build/
dist/
*.egg-info/
.eggs/

# Virtual environments
.venv/
venv/
env/
.env/

# uv caches and environments are local; uv.lock is tracked for the paper.

# Large / regenerable data files
*.npy
*.pt
*.pkl

# Local data storage (root data/ only — src/bondmaxsim/data is a package)
/data/*
!/data/README.md
*.csv
*.png

# results/ is TRACKED — all outputs are research evidence (see docs/project_structure.md).
!results/
!results/**
