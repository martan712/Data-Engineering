# Generated data

This directory is ignored except for this guide. Full embeddings and indexes
are regenerated from pinned public sources and are never committed.

`make data-small` creates `data/fixture/`, a deterministic offline fixture used
by lightweight correctness and reproduction checks. The full-data command
remains disabled until Stage 6 resolves immutable source/model revisions and
freezes the data configuration under `configs/data/`.
